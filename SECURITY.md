# 安全编辑器安全说明

## 保护目标

安全编辑器的目标是避免应用自身在没有用户明确保存时主动把正文写入磁盘，并让程序自有正文缓冲区不进入 Windows 页面文件或 WER 错误报告。正文内存必须完成 `VirtualLock` 和 WER 排除；失败时拒绝继续。释放、关闭、锁屏、挂起、注销和未处理异常路径会尽力在解除保护前清零。

安全内存覆盖：文档 Gap Buffer、打开文件的原始字节、解码缓冲区、保存时的连续 UTF-16 与编码输出、GDI 可见文本 scratch、待配对 UTF-16 输入。行索引、选择位置、编码枚举、换行计数和文件路径不含正文，可使用普通内存。

“复制”和“剪切”是用户明确触发的正文导出操作。选区会直接写入 Win32 `CF_UNICODETEXT` 剪贴板传输内存，不建立普通堆正文副本；传输成功后该内存由 Windows 管理，不属于安全内存覆盖范围。复制失败时，应用会尽力清零尚由自身持有的剪贴板传输内存；剪切只会在复制成功后删除选区。

“粘贴”会读取 Windows 管理的 `CF_UNICODETEXT`，校验 UTF-16、规范化 CRLF/CR 为 LF，并直接复制到临时 `SecureAllocation`，随后再写入安全文档缓冲区。应用不会为粘贴正文建立普通堆副本，临时安全内存在操作结束时清零释放；剪贴板中原有的系统副本不受应用控制。

## 不在保护范围内

安全编辑器无法抵御：

- 管理员、调试器或外部程序主动生成进程转储；
- 内核驱动、内核调试器、完整系统内存转储；
- 休眠文件 `hiberfil.sys`；
- 已失陷的操作系统；
- 截屏、摄像和底层图形捕获；
- GDI、DWM、输入法、字体或显卡驱动产生的瞬时副本；
- 用户明确保存的目标文件被其他程序、备份或云同步读取；
- 用户明确复制或剪切的正文被剪贴板历史、云剪贴板、其他进程或后续粘贴目标读取，且退出应用后仍可能保留；
- 保存期间断电或崩溃导致目标文件截断或损坏。

Windows `LocalDumps` 和外部 `MiniDumpWriteDump` 不受进程内 WER 排除控制。`VirtualLock` 也不等同于防止有权限的进程读取内存。

## 操作建议

- 在受信任且已打补丁的 Windows 10/11 系统上运行；
- 禁用或谨慎使用休眠、系统完整转储和第三方屏幕/输入工具；
- 将目标文件放在受 ACL 和全盘加密保护的位置；
- 保存前确认目标路径，保存后按需要安全管理原文件；
- 复制或剪切敏感正文前确认目标；如不需要剪贴板历史或跨设备同步，请在 Windows 中关闭这些功能，并在使用后覆盖或清除剪贴板；
- 不要把 Wine 冒烟结果当作 Windows 安全语义证明。

## 报告问题

报告安全问题时请只提交最小复现、版本和非敏感测试数据。不要附带真实私钥、Token、密码、转储或包含秘密的截图。项目不实现自动上报、遥测或网络请求。
